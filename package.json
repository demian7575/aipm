{
  "name": "aipm-backend-lambda",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo \"No tests defined\" && exit 0",
    "build": "echo \"Build step - no compilation needed for Node.js\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.932.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@vendia/serverless-express": "^4.12.6",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "serverless": "^3.40.0"
  }
}
