name: Deploy to Production

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      force_deploy:
        description: 'Force deployment even if tests fail'
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  pull-requests: write

jobs:
  # First run gating tests to validate the system
  gating-tests:
    runs-on: ubuntu-latest
    outputs:
      gating-status: ${{ steps.gating.outputs.status }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: |
          npm install --legacy-peer-deps
          sudo apt-get update
          sudo apt-get install -y jq python3-yaml
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      
      - name: Setup SSH key for testing
        env:
          SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          if echo "$SSH_PRIVATE_KEY" | base64 -d > ~/.ssh/id_rsa 2>/dev/null; then
            echo "✅ Decoded base64 SSH key"
          else
            echo "📝 Using raw SSH key"
            printf '%s' "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          fi
          sed -i 's/\r$//' ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H 3.92.96.67 >> ~/.ssh/known_hosts
          ssh-keygen -l -f ~/.ssh/id_rsa >/dev/null 2>&1 && echo "✅ SSH key configured" || (echo "❌ SSH key validation failed" && exit 1)
      
      - name: Run Pre-Production Gating Tests
        id: gating
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          API_BASE: http://3.92.96.67:4000
          SEMANTIC_API_BASE: http://3.92.96.67:8083
          TARGET_ENV: prod
          SSH_HOST: 3.92.96.67
        run: |
          echo "🧪 Running Pre-Production Gating Tests"
          export PHASE="pre-production"
          
          if ./scripts/testing/run-structured-gating-tests.sh; then
            echo "✅ Gating tests passed - production deployment approved"
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "❌ Gating tests failed"
            if [ "${{ github.event.inputs.force_deploy }}" = "true" ]; then
              echo "⚠️ Force deploy enabled - proceeding despite test failures"
              echo "status=success" >> $GITHUB_OUTPUT
            else
              echo "🚫 Blocking production deployment"
              echo "status=failure" >> $GITHUB_OUTPUT
              exit 1
            fi
          fi

  # Only deploy if gating tests pass or force deploy is enabled
  deploy-production:
    runs-on: ubuntu-latest
    needs: gating-tests
    if: needs.gating-tests.outputs.gating-status == 'success'
    environment: production
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install --legacy-peer-deps
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      
      - name: Setup SSH key for deployment
        env:
          SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          # Try base64 decode first, fallback to raw key
          if echo "$SSH_PRIVATE_KEY" | base64 -d > ~/.ssh/id_rsa 2>/dev/null; then
            echo "✅ Decoded base64 SSH key"
          else
            echo "📝 Using raw SSH key"
            printf '%s' "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          fi
          sed -i 's/\r$//' ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H 44.220.45.57 >> ~/.ssh/known_hosts
          ssh-keyscan -H 44.222.168.46 >> ~/.ssh/known_hosts
          ssh-keygen -l -f ~/.ssh/id_rsa >/dev/null 2>&1 && echo "✅ SSH key configured and validated" || (echo "❌ SSH key validation failed" && exit 1)
      
      - name: Deploy to production environment
        run: |
          echo "🚀 Deploying to production environment..."
          
          # Create production config
          echo "📝 Creating production configuration..."
          cat > apps/frontend/public/config.js << EOF
          window.CONFIG = {
            API_BASE_URL: 'http://3.92.96.67:4000',
            EC2_TERMINAL_URL: 'ws://3.92.96.67:8080',
            GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}',
            PROD_VERSION: '4.0.0',
            ENVIRONMENT: 'production',
            environment: 'production',
            stage: 'prod',
            region: 'us-east-1',
            storiesTable: 'aipm-backend-prod-stories',
            acceptanceTestsTable: 'aipm-backend-prod-acceptance-tests',
            DEBUG: false
          };
          EOF
          
          echo "✅ Production config created"
      
      - name: Deploy to production environment
        run: |
          echo "🚀 Deploying to production environment using unified script..."
          ./scripts/deploy-to-environment.sh prod
          
          echo "✅ Backend deployed to production EC2"
      
      - name: Deploy frontend to S3
        run: |
          echo "📦 Deploying frontend to production S3..."
          aws s3 sync apps/frontend/public/ s3://aipm-static-hosting-demo --delete --cache-control no-cache
          echo "✅ Frontend deployed to production S3"
      
      - name: Run Post-Deployment Validation
        id: post_deployment
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          API_BASE: http://3.92.96.67:4000
          SEMANTIC_API_BASE: http://3.92.96.67:8083
          TARGET_ENV: prod
          SSH_HOST: 3.92.96.67
        run: |
          echo "🔍 Running Post-Deployment Validation"
          
          # Wait for deployment to be ready
          sleep 15
          
          # Test production environment health
          echo "Testing production environment..."
          
          # Frontend accessibility
          if curl -s "http://aipm-static-hosting-demo.s3-website-us-east-1.amazonaws.com" | grep -q "AI Project Manager"; then
            echo "✅ Production frontend accessible"
          else
            echo "❌ Production frontend not accessible"
            exit 1
          fi
          
          # Backend API health
          if curl -s "http://44.220.45.57/api/version" | grep -q "version"; then
            echo "✅ Production backend healthy"
          else
            echo "❌ Production backend not healthy"
            exit 1
          fi
          
          # Run post-deployment gating tests
          export PHASE="post-production"
          if ./scripts/testing/run-structured-gating-tests.sh; then
            echo "✅ Post-deployment validation passed"
          else
            echo "❌ Post-deployment validation failed"
            exit 1
          fi
      
      - name: Create deployment summary
        if: always()
        run: |
          echo "## 🚀 Production Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Status**: ${{ steps.post_deployment.outcome == 'success' && '✅ Success' || '❌ Failed' }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Environment URLs:**" >> $GITHUB_STEP_SUMMARY
          echo "- 🔗 Frontend: http://aipm-static-hosting-demo.s3-website-us-east-1.amazonaws.com" >> $GITHUB_STEP_SUMMARY
          echo "- 🔗 Backend API: http://44.220.45.57" >> $GITHUB_STEP_SUMMARY
          echo "- 🔗 Kiro API: http://44.220.45.57:8081" >> $GITHUB_STEP_SUMMARY
