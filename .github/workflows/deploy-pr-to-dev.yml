name: Deploy PR to Development

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to deploy'
        required: false
      branch_name:
        description: 'Branch name to deploy'
        required: false

permissions:
  contents: read
  pull-requests: write

jobs:
  # First run gating tests to validate the system
  gating-tests:
    runs-on: ubuntu-latest
    outputs:
      gating-status: ${{ steps.gating.outputs.status }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Checkout and rebase PR branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # If no PR number or branch provided, use PR context or main
          if [ -z "${{ github.event.inputs.pr_number }}" ] && [ -z "${{ github.event.inputs.branch_name }}" ]; then
            if [ "${{ github.event_name }}" = "pull_request" ]; then
              echo "📥 Auto-deploying PR #${{ github.event.number }}"
              git fetch origin ${{ github.head_ref }}
              git checkout ${{ github.head_ref }}
              BRANCH_NAME="${{ github.head_ref }}"
            else
              echo "📥 No PR specified, using main branch"
              git checkout main
              echo "✅ Using main branch"
              exit 0
            fi
          else
            # Checkout PR branch
            if [ -n "${{ github.event.inputs.branch_name }}" ]; then
              echo "📥 Checking out branch: ${{ github.event.inputs.branch_name }}"
              git fetch origin ${{ github.event.inputs.branch_name }}
              git checkout ${{ github.event.inputs.branch_name }}
              BRANCH_NAME="${{ github.event.inputs.branch_name }}"
            elif [ -n "${{ github.event.inputs.pr_number }}" ]; then
              echo "📥 Checking out PR #${{ github.event.inputs.pr_number }}"
              git fetch origin pull/${{ github.event.inputs.pr_number }}/head:pr-${{ github.event.inputs.pr_number }}
              git checkout pr-${{ github.event.inputs.pr_number }}
              BRANCH_NAME="pr-${{ github.event.inputs.pr_number }}"
            fi
          fi
          
          # Rebase PR onto latest main
          echo "🔄 Rebasing PR onto latest main..."
          git fetch origin main
          
          # Configure git to use non-interactive editor
          git config core.editor true
          
          # Try to rebase
          if GIT_EDITOR=true git rebase origin/main; then
            echo "✅ Rebase successful - proceeding with rebased code"
          else
            echo "❌ Rebase conflict detected - gating tests will fail"
            echo "Please resolve conflicts manually and retry deployment"
            
            # Get PR number
            if [ -n "${{ github.event.inputs.pr_number }}" ]; then
              PR_NUMBER="${{ github.event.inputs.pr_number }}"
            elif [ "${{ github.event_name }}" = "pull_request" ]; then
              PR_NUMBER="${{ github.event.number }}"
            else
              PR_NUMBER="unknown"
            fi
            
            # Notify AIPM backend about rebase failure
            curl -X POST "http://44.220.45.57/api/deployment-notifications" \
              -H "Content-Type: application/json" \
              -d "{
                \"type\": \"rebase_failure\",
                \"prNumber\": \"$PR_NUMBER\",
                \"message\": \"Rebase conflict detected during deployment. Code regeneration needed.\",
                \"timestamp\": \"$(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)\"
              }" || echo "Failed to notify AIPM backend"
            
            exit 1
          fi
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: |
          npm install --legacy-peer-deps
          sudo apt-get update
          sudo apt-get install -y jq python3-yaml
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      
      - name: Copy database files
        run: |
          echo "📁 Copying database files..."
          ./scripts/simple-copy.sh
      
      - name: Run Workflow Gating Tests
        id: gating
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🧪 Running Pre-Deployment Gating Tests on rebased code"
          export PHASE="pre-deployment"
          
          if ./scripts/testing/run-structured-gating-tests.sh; then
            echo "✅ Gating tests passed - deployment approved"
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "❌ Gating tests failed - blocking deployment"
            echo "status=failure" >> $GITHUB_OUTPUT
            exit 1
          fi

  # Only deploy if gating tests pass
  deploy:
    runs-on: ubuntu-latest
    needs: gating-tests
    if: needs.gating-tests.outputs.gating-status == 'success'
    environment: development  # This creates a deployment environment
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Checkout and use rebased code
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Checkout the same PR branch and rebase (should be clean since gating passed)
          if [ -z "${{ github.event.inputs.pr_number }}" ] && [ -z "${{ github.event.inputs.branch_name }}" ]; then
            if [ "${{ github.event_name }}" = "pull_request" ]; then
              git fetch origin ${{ github.head_ref }}
              git checkout ${{ github.head_ref }}
            else
              git checkout main
              exit 0
            fi
          else
            if [ -n "${{ github.event.inputs.branch_name }}" ]; then
              git fetch origin ${{ github.event.inputs.branch_name }}
              git checkout ${{ github.event.inputs.branch_name }}
            elif [ -n "${{ github.event.inputs.pr_number }}" ]; then
              git fetch origin pull/${{ github.event.inputs.pr_number }}/head:pr-${{ github.event.inputs.pr_number }}
              git checkout pr-${{ github.event.inputs.pr_number }}
            fi
          fi
          
          # Rebase again (should be clean since gating tests passed)
          git fetch origin main
          git config core.editor true
          GIT_EDITOR=true git rebase origin/main
          
          echo "✅ Using rebased code for deployment"
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install --legacy-peer-deps
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      
      - name: Setup SSH key for deployment
        env:
          SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H 44.222.168.46 >> ~/.ssh/known_hosts
          
          # Validate SSH private key format
          if ssh-keygen -y -f ~/.ssh/id_rsa > /dev/null 2>&1; then
            echo "✅ SSH private key is valid"
          else
            echo "❌ SSH private key validation failed"
            exit 1
          fi
          
          echo "✅ SSH key configured for development deployment"
      
      - name: Copy database files
        run: |
          echo "📁 Copying database files..."
          ./scripts/simple-copy.sh
      
      - name: Deploy to development environment
        env:
          SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
        run: |
          echo "🚀 Deploying to development environment using unified script..."
          echo "📍 Current branch: $(git branch --show-current)"
          ./scripts/deploy-to-environment.sh dev
      
      - name: Run production gating tests
        run: |
          echo "🧪 Running production gating tests..."
          
          # Wait for deployment to be ready
          sleep 10
          
          echo "✅ Deployment completed"
      
      - name: Run Post-Deployment Validation
        id: post_deployment
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🔍 Running Post-Deployment Validation"
          export PHASE="post-deployment"
          
          # Run post-deployment gating tests
          if ./scripts/testing/run-structured-gating-tests.sh; then
            echo "✅ Post-deployment validation passed"
          else
            echo "❌ Post-deployment validation failed"
            exit 1
          fi
      
      - name: Validate Data Consistency
        run: |
          echo "📊 Validating data consistency after deployment..."
          ./scripts/testing/post-deployment-data-consistency.sh
      
      - name: Comment on PR
        if: always()
        uses: actions/github-script@v6
        with:
          script: |
            const success = '${{ steps.post_deployment.outcome }}' === 'success';
            const status = success ? '✅ **Deployment Successful**' : '❌ **Deployment Failed**';
            const emoji = success ? '🚀' : '⚠️';
            
            const body = `${emoji} **Deployed to Development Environment**
            
            ${status}
            
            **Deployment Steps:**
            - ✅ Pre-deployment gating tests passed
            - ✅ Frontend deployed to S3
            - ✅ Backend deployed to EC2
            - ✅ Development config updated
            - ✅ Data synchronized from production
            - ${success ? '✅' : '❌'} Post-deployment validation ${success ? 'passed' : 'failed'}
            
            **Environment URLs:**
            - 🔗 Frontend: http://aipm-dev-frontend-hosting.s3-website-us-east-1.amazonaws.com
            - 🔗 Backend API: http://44.222.168.46
            - 🔗 Kiro API: http://44.222.168.46:8081
            
            ${success ? '**Ready for testing!** 🎉' : '**Please check logs and retry deployment** 🔧'}`;
            
            if (context.payload.pull_request) {
              github.rest.issues.createComment({
                issue_number: context.payload.pull_request.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: body
              });
            } else {
              const prNumber = '${{ github.event.inputs.pr_number }}';
              if (prNumber) {
                github.rest.issues.createComment({
                  issue_number: parseInt(prNumber),
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  body: body
                });
              }
            }
