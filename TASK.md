# Add story count badge

## Requirements
Display total story count as a badge in the header

## Constraints
Vanilla JavaScript, update count on story changes

## Acceptance Criteria
- Badge shows in header
- Count updates dynamically
- Styled with CSS

## Implementation
⚠️ Code generation failed. Implement manually.
