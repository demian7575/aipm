# Add story counter badge

## Requirements
Display total story count in header as a badge

## Constraints
Vanilla JavaScript, minimal CSS

## Acceptance Criteria
- Badge shows total count
- Updates on story add/delete
- Styled with CSS

## Implementation Notes
**⚠️ This PR requires manual implementation or use Amazon Q locally:**

```bash
# In your local environment:
git checkout feature-counter-1764412593999
q chat "Implement: Add story counter badge. Display total story count in header as a badge"
git add -A
git commit -m "feat: implement Add story counter badge"
git push
```

Then review and merge this PR.
